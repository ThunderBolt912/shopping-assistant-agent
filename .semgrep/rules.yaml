rules:
  - id: hardcoded-google-api-key
    patterns:
      - pattern-regex: "\"AIzaSy[A-Za-z0-9_-]*\""
    message: "Hardcoded Google API key detected. Store secrets securely and never commit them."
    languages: [python]
    severity: ERROR
